# 강사 출강 운영 플랫폼 PRD

> 문서 상태: Draft v0.7
> 작성일: 2026-08-08  
> 마지막 요구사항 변경: 2026-08-12
> 초기 운영 대상: 15명 내외의 소속 강사를 관리하는 단일 교육업체

## v0.4 관리자 인증·권한 요구사항

- 운영 최고관리자 및 교육업체 표시 이름은 `NGN-X`, 로그인 이메일은 `nugoona2021@naver.com`으로 한다.
- 운영 인증과 관계형 데이터 저장소는 Supabase, 애플리케이션 배포는 Vercel을 사용한다.
- 최고관리자는 사전 생성된 이메일·비밀번호 계정으로 로그인하며 로그인 과정에서 별도의 이메일 인증을 요구하지 않는다. 지정 이메일의 최초 생성 시에만 `super_admin` 권한을 자동 부여한다.
- 역할은 `super_admin`, `service_admin`, `company_member`, `instructor`로 구분한다.
- 최고관리자는 회원 승인·거절, 역할 변경, 서비스 관리자 지정 권한을 가진다.
- 서비스 관리자는 일반 회원 승인과 역할 변경을 수행할 수 있으나 최고관리자 변경 및 서비스 관리자 추가 지정은 할 수 없다.
- 관리자 화면과 API는 서버 인증과 Row Level Security를 모두 통과한 사용자에게만 제공한다.
- 관리자 권한 변경은 행위자, 대상, 변경 내용을 감사 로그로 남긴다.

## v0.5 강사 회원가입·로그인 요구사항

- 강사용 회원가입과 로그인 화면은 관리자 운영센터와 별도 경로로 제공한다.
- 강사는 실명, 이메일, 비밀번호를 입력해 가입하며 이메일 확인 후 `instructor / pending` 상태가 된다.
- 관리자가 승인한 `active` 강사만 강사센터의 전체 기능을 이용할 수 있다.
- 가입·로그인 화면은 모바일 우선 반응형 UI와 접근 가능한 폼 레이블을 제공한다.
- 강사 권한은 클라이언트 메타데이터가 아니라 데이터베이스 프로필과 RLS를 기준으로 판정한다.

## v0.6 운영센터 설정·관리자 보안 요구사항

- 운영센터의 모든 화면에서 현재 계정을 명확하게 로그아웃할 수 있어야 한다.
- 설정 화면은 `관리자 관리`와 `보안·활동 기록` 메뉴로 구분한다.
- 최고관리자는 7일 유효 보안 링크로 운영관리자를 초대하고, 운영관리자의 권한 해제·이용 중지·이용 재개를 수행할 수 있다.
- 최고관리자는 최소 1명을 유지하며 최고관리자 계정은 일반 권한 변경·이용 중지 동작에서 제외한다.
- 운영관리자는 수업 등록, 알림 발송, 강사 배정 기능을 사용하되 다른 관리자 계정을 변경할 수 없다.
- 관리자는 본인의 비밀번호를 변경하고 로그인된 기기를 확인하며 현재 기기를 제외한 다른 세션을 종료할 수 있다.
- 활동 기록은 관리자 로그인, 관리자 초대·권한·상태 변경, 수업 생성·수정·취소, 배정 요청·재알림·확정·변경·취소만 저장한다.
- 활동 기록은 작업 일시, 작업 관리자, 작업 내용을 표시하고 기간과 관리자 기준으로 조회할 수 있어야 한다.

## v0.7 알림 채널·실시간 응답 운영 요구사항

- MVP 알림은 `플랫폼 내부 알림 → 무료 웹 푸시 → 카카오톡 공유`의 3개 운영 채널을 사용하고, 카카오 알림톡은 향후 선택 가능한 4단계 확장 채널로 둔다.
- 내부 알림은 모든 요청·재알림·배정 결과의 기본 원장으로 사용하며 웹 푸시 권한이나 발송 성공 여부와 무관하게 먼저 저장한다.
- 웹 푸시는 사용자가 브라우저 알림 권한을 허용하고 구독한 기기에 즉시 발송한다. 미지원·미허용·미구독 사용자는 내부 알림으로 계속 확인할 수 있어야 한다.
- 카카오톡 공유는 관리자가 실제 수업 정보와 응답 링크가 포함된 안내문을 기기 공유 기능으로 전달하거나 복사하는 보조 수단이며 자동 발송 채널로 취급하지 않는다.
- 알림톡은 MVP 필수 의존성에서 제외하고, 향후 도달률·운영비·고객 요구를 확인한 뒤 공급자 어댑터로 선택 연동한다.
- 관리자 응답 현황은 Supabase의 실제 모집 대상·역할별 응답을 기준으로 요청·가능·조건부·불가능·미응답 수, 조건, 응답 시각, 역할별 후보 충족 여부를 표시한다.
- 재알림은 미응답 역할이 남은 강사를 관리자가 선택해 발송하며 내부 알림 저장, 웹 푸시 발송 결과, 최근 재알림 시각과 관리자 활동 이력을 보존한다.

## 1. 문서 목적

이 문서는 학교·기관 출강 수업을 운영하는 교육업체를 위한 강사 배정 플랫폼의 제품 요구사항을 정의한다. 초기 제품은 종합 교육 ERP가 아니라, 단체 카카오톡과 개별 연락에 의존하던 강사 배정 과정을 구조화하여 관리자의 반복 연락을 줄이는 데 집중한다.

## 2. 제품 개요

### 2.1 한 줄 정의

15명 안팎의 강사를 관리하는 교육업체가 출강 요청을 플랫폼 내부 알림과 무료 웹 푸시로 전달하고, 기한 내 응답을 수집하여 관리자가 빠르게 강사를 확정하는 웹 기반 배정 플랫폼이다.

### 2.2 제품 포지셔닝

초기 제품은 다음과 같이 정의한다.

> 소규모 교육업체를 위한 강사 출강 요청·응답·배정 관리 도구

수업 준비, 출석, 수업일지, 출강료 정산, 월간 보고서는 장기적으로 연결하되 초기 MVP의 핵심 검증 대상에서는 제외한다.

## 3. 배경 및 문제 정의

### 3.1 현재 운영 방식

1. 학교나 기관으로부터 출강 요청을 받는다.
2. 관리자가 수업 일정과 내용을 단체 카카오톡방에 공지한다.
3. 소속 강사들에게 출강 가능 여부를 조사한다.
4. 응답이 늦거나 없는 강사에게 관리자가 개별적으로 연락한다.
5. 가능하다고 응답한 강사 중 관리자가 최종 강사를 선택한다.
6. 적합한 강사를 확보하지 못하면 수업이 지연되거나 취소될 수 있다.

### 3.2 핵심 문제

> 단체 카카오톡 공지는 응답 기한, 응답 상태, 일정 충돌 여부를 체계적으로 관리하기 어려워 관리자가 미응답자를 반복적으로 확인하고 개별 연락해야 한다.

핵심 문제는 단순히 강사가 부족한 것이 아니라, 강사별 응답 상태가 한곳에 정리되지 않아 관리자가 지속적으로 연락해야 한다는 점이다.

### 3.3 주요 불편

- 여러 카카오톡 메시지에서 강사별 응답을 다시 찾아야 한다.
- 공지를 확인했는지, 아직 응답하지 않았는지 구분하기 어렵다.
- 수업마다 응답 마감 기준이 다르지만 이를 일관되게 관리하기 어렵다.
- 기존 확정 수업과 새 수업의 시간 중복을 사람이 확인해야 한다.
- 재알림 대상자를 관리자가 일일이 선별하고 연락해야 한다.
- 배정 완료 후 선택·미선택 결과를 각각 안내해야 한다.
- 응답 지연으로 강사 배정이 늦어지거나 수업이 취소될 수 있다.

## 4. 제품 목표 및 비목표

### 4.1 핵심 목표

> 한 번의 배정 요청과 필요한 최소한의 재알림만으로 강사 후보를 확보하고, 관리자가 빠르게 최종 배정할 수 있게 한다.

### 4.2 제품 가설

단체 카카오톡 공지를 개인별 플랫폼 알림·웹 푸시와 웹 응답 화면으로 대체하고, 모든 응답 상태를 관리자 현황판에 자동 집계하면 관리자의 개별 연락 횟수가 유의미하게 감소할 것이다.

### 4.3 주요 성공지표

북극성 지표는 `수업 1건당 관리자의 개별 연락 횟수`로 정의한다.

| 지표 | 정의 | 초기 목표 |
| --- | --- | --- |
| 수업당 개별 연락 횟수 | 전화, 개인 카카오톡, 개별 문자 등 관리자가 직접 수행한 연락 수 | 기존 대비 70% 이상 감소 |
| 일괄 알림 횟수 | 최초 배정 요청 발송 횟수 | 수업당 1회 |
| 선택 재알림 횟수 | 관리자가 미응답자를 선택해 다시 발송한 횟수 | 수업당 1회 이내 권장 |
| 수동 연락 횟수 | 플랫폼 외부에서 진행한 개별 연락 수 | 수업당 0~2회 이내 |

목표치는 초기 가설이며, 도입 전에 현재 평균 연락 횟수를 기준선으로 측정한 후 조정한다.

### 4.4 비목표

- 시스템이 관리자를 대신해 최종 강사를 자동 결정하지 않는다.
- 초기 단계에서 학교·기관이 직접 수업을 신청하도록 만들지 않는다.
- 초기 단계에서 프리랜서 공개 모집이나 강사 거래 시장을 만들지 않는다.
- 월 10건 미만의 운영 규모에서 불필요한 대규모 자동화 기능을 우선 개발하지 않는다.

## 5. 사용자 및 권한

### 5.1 초기 사용자

- 하나의 교육업체 내부 관리자
- 교육업체 소속 강사 약 15명
- 평균 수업 배정 건수 월 10건 미만
- 학교·기관 담당자는 초기 사용자에 포함하지 않음

### 5.2 사용자 역할

| 역할 | 설명 | 주요 권한 |
| --- | --- | --- |
| 최고 관리자 | 플랫폼과 소속 회원을 총괄하는 사용자 | 회원 승인, 관리자 지정, 강사 관리, 전체 수업·배정 현황 조회, 설정 관리 |
| 운영 관리자 | 실제 수업과 배정을 운영하는 사용자 | 수업 등록·수정, 요청 대상 강사 선택, 알림 발송, 재알림, 최종 배정 |
| 소속 강사 | 교육업체에 소속되어 출강 요청을 받는 사용자 | 배정 요청 응답, 본인의 확정 일정 및 수업 정보 조회 |
| 비소속 강사 | 향후 외부 출강 기회를 받을 수 있는 프리랜서 | 초기 화면과 매칭 기능에서는 제외하되 확장 가능한 역할 구조만 준비 |

### 5.3 회원 정책

- 사용자는 회원가입 후 관리자의 승인을 받아야 한다.
- 회원 상태는 `승인 대기`, `활성`, `이용 중지`로 구분한다.
- 소속 여부와 역할을 분리해 저장한다.
- 프리랜서 확장을 고려해 `소속 강사`와 `비소속 강사`를 구분할 수 있어야 한다.
- 초기 서비스에서 비소속 강사의 공개 가입·매칭 기능은 제공하지 않는다.

## 6. 핵심 사용자 여정

### 6.1 전체 서비스 흐름

`로그인 → 일정 조율 → 강사 배정 → 수업 확정 → 수업 준비 → 수업 진행 → 비용 정산`

초기 MVP는 이 가운데 `로그인 → 일정 조율 → 강사 배정 → 수업 확정`을 우선 구현한다.

### 6.2 MVP 배정 흐름

1. 관리자가 수업 정보와 역할별 필요 인원(`주강사 1명`, `보조강사 0~2명`)을 등록한다.
2. 시스템이 강사들의 플랫폼 내 확정 수업을 기준으로 일정 충돌을 확인한다.
3. 관리자가 요청을 보낼 소속 강사와 각 강사에게 모집할 역할(`주강사`, `보조강사` 또는 `두 역할 모두`)을 선택한다.
4. 관리자가 수업별 응답 마감 일시를 설정한다.
5. 선택한 강사에게 모집 역할이 명시된 플랫폼 내부 알림을 저장하고, 웹 푸시 구독 기기에는 같은 요청을 즉시 발송한다. 관리자는 필요한 경우 카카오톡 공유 안내문을 보조 수단으로 사용할 수 있다.
6. 강사는 요청받은 역할별로 `가능`, `불가능`, `조건부 가능` 중 하나로 응답한다.
7. 조건부 가능을 선택한 강사는 조건이나 사유를 입력한다.
8. 관리자 현황판에 주강사·보조강사 후보와 응답 상태가 역할별로 실시간 집계된다.
9. 관리자는 필요한 경우 미응답자를 선택해 재알림을 보낸다.
10. 관리자가 응답한 후보 중 주강사 1명과 필요한 보조강사 0~2명을 직접 선택한다.
11. 선택된 강사와 미선택 강사에게 결과를 안내한다.
12. 선택된 강사의 확정 일정에 해당 수업과 배정 역할을 등록한다.
13. 소속 강사만으로 배정하지 못하면 향후 비소속 강사 요청으로 확대할 수 있다.

## 7. MVP 기능 요구사항

### 7.1 인증 및 회원 승인

#### 필수 요구사항

- 이메일 또는 휴대전화 기반으로 회원가입할 수 있어야 한다.
- 관리자는 승인 대기 회원의 정보를 확인하고 승인 또는 거절할 수 있어야 한다.
- 역할과 소속 상태에 따라 접근 가능한 메뉴와 데이터가 달라야 한다.
- 이용 중지된 회원은 새로운 배정 요청을 받을 수 없어야 한다.
- 관리자는 강사의 이름, 연락처, 담당 가능 과목, 대상 학년, 활동 가능 지역을 관리할 수 있어야 한다.

### 7.2 수업 등록 및 관리

#### 필수 입력 항목

- 기관명
- 기관 담당자 정보
- 수업 날짜
- 시작·종료 시간
- 장소 및 주소
- 수업 대상
- 학년
- 참여 인원
- 수업 내용
- 역할별 필요 강사 수: 주강사 1명(필수), 보조강사 0~2명(선택)
- 주강사 수업료: 주강사 1인에게 지급할 금액(원화, 필수)
- 보조강사 수업료: 보조강사 1인당 지급할 금액(원화, 보조강사 필요 인원이 1명 이상일 때 필수)
- 수업료 안내사항: 교통비 포함 여부, 원천징수, 지급 예정일 등 강사에게 안내할 내용(선택)
- 수업 관련 참고사항
- 응답 마감 일시

#### 수업료 입력 규칙

- 역할별 수업료는 0원 이상의 원화 정수로 입력하고 화면에는 천 단위 구분 기호와 `원` 단위를 표시한다.
- 보조강사가 여러 명이어도 보조강사 수업료는 각 보조강사에게 동일하게 적용되는 1인당 금액으로 저장한다.
- MVP에서는 역할별 수업료를 등록·안내·기록하지만, 세금·교통비 계산이나 실제 지급 처리는 수행하지 않는다.

#### 상태

- 작성 중
- 배정 요청 전
- 응답 대기
- 배정 필요
- 배정 완료
- 취소

#### 동작

- 관리자는 수업 정보를 등록·수정·복제할 수 있어야 한다.
- 배정 요청이 발송된 뒤 핵심 일정 정보 또는 역할별 수업료가 변경되면 요청받은 강사에게 변경 전·후 내용과 변경 사실을 안내해야 한다.
- 수업이 취소되면 배정된 강사와 응답한 강사에게 취소 알림을 보낼 수 있어야 한다.

### 7.3 일정 충돌 확인

- 시스템은 플랫폼에 등록된 강사의 확정 수업만을 기준으로 충돌을 확인한다.
- 외부 개인 일정과 캘린더는 초기 버전에서 연동하지 않는다.
- 시간이 일부라도 겹치면 충돌로 표시한다.
- 충돌이 있는 강사를 요청 대상에서 자동 제외하지 않고, 관리자에게 경고한 뒤 선택 여부를 맡긴다.
- 강사에게도 응답 화면에서 충돌 가능성을 보여준다.

### 7.4 요청 대상 강사 선택

- 초기에는 소속 강사를 우선 대상으로 한다.
- 관리자는 요청 대상을 직접 선택할 수 있어야 한다.
- 강사 목록에서 담당 가능 과목, 학년, 지역, 일정 충돌 여부를 확인할 수 있어야 한다.
- 한 수업의 최종 배정 인원은 최소 1명, 최대 3명으로 제한한다.
- 모든 수업에는 주강사 1명을 필수로 배정하고, 보조강사는 수업에 따라 0~2명을 배정할 수 있어야 한다.
- 관리자는 요청 대상 강사별로 모집 역할을 `주강사`, `보조강사`, `두 역할 모두` 중 하나로 지정할 수 있어야 한다.
- 한 강사를 두 역할 모두의 후보로 모집할 수 있지만, 최종 배정 시에는 한 수업에서 하나의 역할만 부여할 수 있다.
- 요청 발송 전에 역할별 필요 인원, 역할별 요청 대상, 역할별 수업료, 마감 시간, 수업 핵심 정보를 최종 확인할 수 있어야 한다.

### 7.5 내부 알림·웹 푸시·카카오톡 공유

- 배정 요청, 재알림, 배정 결과는 먼저 플랫폼 내부 알림으로 저장한다.
- 내부 알림은 로그인한 본인만 조회할 수 있고 읽음 시각을 기록한다.
- 웹 푸시는 HTTPS 환경의 서비스 워커와 VAPID 구독을 사용하며 사용자의 명시적 알림 권한 허용 이후에만 활성화한다.
- 웹 푸시가 미지원·미허용·미구독이거나 발송에 실패해도 내부 알림과 수업 요청 화면은 정상 동작해야 한다.
- 웹 푸시 구독 정보는 사용자별로 저장하고 해당 사용자와 권한이 확인된 서버 발송 경로에서만 사용한다.
- 관리자는 실제 수업 정보로 생성된 안내문을 운영체제 공유 시트에서 카카오톡으로 공유할 수 있다. 공유 시트를 사용할 수 없으면 클립보드 복사를 제공한다.
- 카카오 알림톡은 MVP 필수 기능이 아니며 향후 `NotificationProvider` 어댑터로 선택 연동한다.
- 알림 본문에는 상세 개인정보를 과도하게 노출하지 않는다.

#### 알림 핵심 정보

- 기관명 또는 식별 가능한 수업명
- 날짜와 시간
- 지역
- 대상·학년
- 모집 역할(주강사·보조강사 또는 두 역할 모두)
- 역할별 모집 인원
- 요청받은 역할에 해당하는 수업료와 수업료 안내사항
- 응답 마감 시간
- 응답하기 버튼

### 7.6 강사 응답

#### 응답 선택지

- 가능
- 불가능
- 조건부 가능

강사는 자신이 요청받은 모집 역할과 역할별 수업료를 확인한 뒤 역할별로 응답한다. `두 역할 모두`로 요청받은 경우 주강사·보조강사의 수업료를 각각 확인하고 서로 다른 응답을 제출할 수 있어야 한다.

#### 조건부 가능

- 조건부 가능을 선택했을 때만 사유 입력란을 노출한다.
- 강사는 시간 조정, 이동 조건, 준비 조건 등 필요한 내용을 자유롭게 입력할 수 있어야 한다.

#### 기타 동작

- 강사는 응답 마감 전까지 본인의 응답을 변경할 수 있어야 한다.
- 응답 변경 내역과 역할별 최종 응답 시각을 기록한다.
- 배정이 끝난 요청은 결과와 본인의 상태를 조회할 수 있어야 한다.
- 응답 완료 후 성공 상태와 다음 안내를 명확히 표시한다.

### 7.7 관리자 응답 현황판

관리자는 수업별로 다음 상태를 한 화면에서 확인할 수 있어야 한다.

| 상태 | 표시 정보 |
| --- | --- |
| 가능 | 모집 역할, 강사명, 응답 시각, 일정 충돌 여부 |
| 조건부 가능 | 모집 역할, 강사명, 응답 시각, 조건 및 사유 |
| 불가능 | 모집 역할, 강사명, 응답 시각 |
| 미응답 | 모집 역할, 강사명, 최초·최근 알림 발송 시각 |
| 일정 충돌 | 겹치는 확정 수업과 시간 |
| 마감 경과 | 기한 내 미응답 여부 |

#### 필수 동작

- 응답 상태별 필터링
- 모집 역할별 필터링 및 주강사·보조강사 후보 분리 조회
- 강사명 검색
- 마감 임박 순 정렬
- 미응답자 선택
- 선택한 미응답자에게 재알림 발송
- 역할별 가능·조건부 가능 후보 비교
- 주강사 1명과 보조강사 0~2명의 필요 인원 대비 역할별 확보 후보 수 표시

### 7.8 재알림 정책

- 재알림은 시스템이 자동 반복 발송하지 않는다.
- 관리자가 대상자를 선택해 직접 발송한다.
- 이미 응답한 강사는 기본 선택 대상에서 제외한다.
- 최근 재알림 발송 시간을 표시해 중복 연락을 방지한다.
- 내부 알림을 먼저 저장한 뒤 웹 푸시를 시도하며, 푸시 미구독 강사도 내부 알림에서 확인할 수 있어야 한다.
- 재알림 대상, 요청 식별키, 발송 시각, 관리자 활동 이력을 저장해 중복 실행을 방지한다.

### 7.9 최종 배정

- 시스템은 강사를 자동 확정하지 않는다.
- 관리자가 가능·조건부 가능 후보 중 최종 강사를 직접 선택한다.
- 주강사는 정확히 1명을 필수로 선택해야 한다.
- 보조강사는 수업에 설정된 필요 인원에 따라 0~2명을 선택할 수 있어야 한다.
- 한 수업의 전체 배정 인원은 1~3명이어야 한다.
- 동일 강사를 같은 수업의 주강사와 보조강사로 중복 배정할 수 없다.
- 강사가 해당 역할에 `가능` 또는 `조건부 가능`으로 응답한 경우에만 그 역할로 배정할 수 있다.
- 조건부 가능 강사를 선택할 때 조건 내용을 다시 확인시킨다.
- 일정 충돌이 있는 강사를 선택할 때 경고를 보여준다.
- 배정 확정 전에 역할별 필요 인원 충족 여부와 선택된 강사에게 적용될 역할별 수업료를 함께 확인한다.
- 배정 확정 후 해당 수업과 배정 역할을 강사의 확정 일정에 등록한다.

### 7.10 결과 알림

- 선택된 강사에게 주강사·보조강사 중 본인의 배정 역할과 해당 역할의 수업료가 명시된 확정 알림을 보낸다.
- 응답했지만 선택되지 않은 강사에게 모집 종료 또는 미선택 결과를 알린다.
- 미응답자에게도 요청 종료 상태가 반영되어야 한다.
- 배정 후 일정이 변경되거나 취소되면 대상 강사에게 변경 알림을 보낼 수 있어야 한다.

### 7.11 관리자 홈

관리자 홈에서는 다음 네 가지 정보를 우선 제공한다.

- 응답 대기 수업
- 마감 임박 수업
- 강사 배정이 필요한 수업
- 오늘의 확정 수업

초기에는 복잡한 경영 통계보다 즉시 조치가 필요한 업무를 우선 노출한다.

## 8. 화면 구조

### 8.1 관리자 정보구조

- 홈
- 수업
- 배정 요청
- 일정
- 강사
- 회원 승인
- 설정

### 8.2 핵심 관리자 화면

1. 로그인
2. 관리자 홈
3. 수업 목록
4. 수업 등록·수정
5. 요청 대상 강사 선택
6. 요청 발송 전 확인
7. 수업별 응답 현황
8. 최종 배정
9. 전체 일정
10. 강사 목록·상세
11. 회원 승인
12. 알림 발송 이력

### 8.3 핵심 강사 화면

1. 내부 알림 또는 웹 푸시에서 강사센터 진입
2. 수업 요약 및 상세 확인
3. 가능·불가능·조건부 가능 선택
4. 조건부 가능 사유 입력
5. 응답 완료
6. 배정 결과 확인
7. 본인의 확정 일정

## 9. 디자인 방향

### 9.1 디자인 원칙

- 관리자가 3초 안에 미응답·마감 임박 수업을 발견할 수 있어야 한다.
- 강사가 알림톡 진입 후 10초 안에 응답을 완료할 수 있어야 한다.
- 정보량은 충분하되 장식 요소로 판단을 방해하지 않는다.
- 상태를 색상만으로 표현하지 않고 아이콘과 텍스트를 함께 사용한다.
- 관리자와 강사의 사용 환경을 분리해 설계한다.

### 9.2 관리자 화면

- 데스크톱 우선 반응형 웹
- 밝고 차분한 B2B SaaS 스타일
- 왼쪽 고정 내비게이션
- 목록과 상태 중심의 운영 화면
- 행 또는 카드를 선택하면 상세 패널이 열리는 구조
- `수업 목록`과 `주간 일정` 전환 제공
- 정보 밀도는 높되 경계선, 여백, 타이포그래피로 위계를 구분

### 9.3 강사 화면

- 모바일 우선 웹
- 한 화면에서 하나의 주요 행동에 집중
- 큰 제목과 쉬운 문장 사용
- 넓고 명확한 응답 버튼
- 핵심 수업 정보를 먼저 보여주고 부가정보는 접어서 제공
- 화면 하단에 응답 버튼을 고정하는 방식 고려
- 응답 제출과 변경 결과를 명확하게 안내

### 9.4 권장 비주얼 시스템

| 항목 | 권장 방향 |
| --- | --- |
| 기본 배경 | 밝은 회백색 `#F7F8FA` |
| 카드·패널 | 흰색 |
| 주요 색상 | 신뢰감 있는 블루 `#3563E9` |
| 보조 색상 | 부드러운 민트 또는 청록 |
| 가능·성공 | 초록색 + 체크 아이콘 + 상태명 |
| 조건부 가능 | 주황색 + 설명 아이콘 + 상태명 |
| 불가능·충돌 | 빨간색 + 경고 아이콘 + 상태명 |
| 미응답 | 회색 + 시계 아이콘 + 상태명 |
| 글꼴 | Pretendard 또는 SUIT |
| 모서리 | 10~12px 라운드 |
| 구분 방식 | 강한 그림자보다 1px 경계선 중심 |
| 다크 모드 | 초기 범위에서 제외 |

### 9.5 디자인 레퍼런스

1. [Connecteam Scheduling](https://connecteam.com/employee-scheduling-app/): 일정, 가용성, 충돌, 배정 상태의 기능 구조
2. [Linear](https://linear.app/): 관리자 대시보드, 상태 중심 목록, 빠른 업무 처리 경험
3. [Toss Design System](https://developers-apps-in-toss.toss.im/design/components): 모바일 응답 화면, 쉬운 문장, 명확한 행동과 컴포넌트 일관성

권장 조합은 `Connecteam의 일정·배정 구조 + Linear의 관리자 대시보드 + Toss의 모바일 응답 경험`이다.

## 10. 핵심 비즈니스 규칙

1. 배정 요청은 소속 강사에게 우선 전달한다.
2. 소속 강사만으로 충원하지 못했을 때만 향후 비소속 강사로 확대한다.
3. 응답 마감은 수업마다 관리자가 직접 설정한다.
4. 재알림 대상과 시점은 관리자가 결정한다.
5. 강사의 응답은 가능·불가능·조건부 가능으로 구분한다.
6. 조건부 가능에는 사유가 반드시 포함되어야 한다.
7. 수업별 필요 인원은 주강사 1명과 보조강사 0~2명으로 구성하며 전체 1~3명이어야 한다.
8. 모집 요청, 강사 응답, 후보 비교, 최종 배정 및 결과 알림에서 주강사와 보조강사를 구분한다.
9. 한 강사는 같은 수업에서 하나의 역할로만 최종 배정할 수 있다.
10. 주강사 수업료와 보조강사 1인당 수업료를 분리해 등록하며 모집 요청부터 최종 배정까지 해당 역할의 금액을 일관되게 표시한다.
11. 역할별 수업료 변경은 변경 전·후 금액과 변경 시각을 기록하고 요청받은 강사에게 안내한다.
12. 최종 배정은 관리자가 결정한다.
13. 기존 확정 일정과 겹치는 강사는 경고하되 자동 제외하지 않는다.
14. 배정이 확정되면 관련된 모든 강사에게 결과를 안내한다.
15. 모든 요청, 역할별 응답, 재알림, 배정 변경은 이력으로 남긴다.

## 11. 데이터 요구사항

### 11.1 주요 데이터 객체

- 업체
- 회원
- 강사 프로필
- 기관
- 기관 담당자
- 수업 및 역할별 수업료
- 배정 요청
- 요청 대상 강사 및 모집 역할
- 역할별 강사 응답
- 배정 결과, 배정 역할 및 적용 수업료
- 확정 일정
- 알림 및 발송 이력

### 11.2 필수 이력

- 수업 생성·수정·취소 이력과 역할별 수업료 변경 이력
- 배정 요청 발송자와 발송 시각
- 요청 대상 강사 목록과 강사별 모집 역할
- 강사별·역할별 최초·최종 응답과 변경 시각
- 재알림 대상자와 발송 시각
- 최종 배정자, 배정 역할, 적용 수업료와 배정 시각
- 배정 변경·취소 이력

## 12. 비기능 요구사항

### 12.1 사용성

- 강사 응답은 모바일 브라우저에서 별도 앱 설치 없이 완료할 수 있어야 한다.
- 주요 업무는 가능한 한 한 화면 또는 최소 단계로 완료할 수 있어야 한다.
- 날짜, 시간, 마감 상태는 한국어와 한국 표준시 기준으로 명확히 표시한다.

### 12.2 보안 및 개인정보

- 역할에 따라 접근 가능한 데이터 범위를 제한한다.
- 개인별 응답 링크는 추측하기 어려운 토큰과 유효기간을 사용한다.
- 링크 유출에 대비해 민감한 개인정보 노출을 최소화한다.
- 관리자 작업과 배정 변경 이력을 기록한다.
- 연락처 등 개인정보는 필요한 사용자에게만 표시한다.

### 12.3 신뢰성

- 중복 알림 발송을 방지해야 한다.
- 알림 발송 성공·실패 상태를 관리자가 확인할 수 있어야 한다.
- 동일 강사를 같은 시간대의 두 확정 수업에 배정하려 할 때 경고해야 한다.
- 동일 강사를 같은 수업의 주강사와 보조강사로 중복 배정하지 않아야 한다.
- 주강사 없이 배정을 확정할 수 없어야 하며, 전체 배정 인원이 3명을 초과할 수 없어야 한다.
- 응답과 배정 확정 과정은 중복 제출에도 일관된 결과를 유지해야 한다.

### 12.4 접근성

- 상태를 색상만으로 구분하지 않는다.
- 버튼과 입력 영역은 모바일 터치에 충분한 크기로 제공한다.
- 텍스트와 배경의 명도 대비를 확보한다.
- 오류는 원인과 해결 방법을 문장으로 안내한다.

## 13. 초기 범위에서 제외되는 기능

- 학교·기관 담당자 회원가입 및 직접 수업 신청
- 비소속 프리랜서 강사 공개 모집·검색·매칭
- AI 기반 최적 강사 추천 및 자동 배정
- 구글·네이버·애플 등 외부 캘린더 연동
- 강사의 개인 일정 등록 및 반복 불가 일정 관리
- 출석 관리
- 상세 수업일지
- 수업안과 준비물 협업 관리
- 등록된 역할별 수업료를 이용한 세금·교통비·총액 등의 자동 계산
- 입금 및 지급 관리
- 월간 실적 보고서
- 모바일 네이티브 앱
- 다크 모드

## 14. 단계별 확장 계획

### Phase 1. 요청·응답·배정 MVP

- 관리자 승인 가입
- 소속 강사 관리
- 수업 등록
- 역할별 필요 인원과 수업료 설정 및 주강사·보조강사 구분 모집
- 확정 일정 충돌 확인
- 플랫폼 내부 알림 및 무료 웹 푸시 요청
- 관리자 카카오톡 공유 보조 기능
- 가능·불가능·조건부 가능 응답
- 응답 현황판
- 선택 재알림
- 관리자 최종 배정
- 결과 알림 및 확정 일정 등록

### Phase 2. 수업 준비 및 진행

- 수업안 등록·공유
- 준비물 체크리스트
- 수업 전 자동 알림
- 출석 관리
- 수업일지 작성
- 사진·파일 첨부

### Phase 3. 비용 정산 및 보고

- 강사별 출강료 기준 관리
- 수업별 출강료 계산
- 지급·입금 상태 관리
- 월별 강사 실적
- 기관별 수업 실적
- 월간 보고서 생성 및 내보내기

### Phase 4. 외부 강사 및 기관 확장

- 비소속 프리랜서 강사 가입과 검증
- 소속 강사 미배정 시 외부 강사로 요청 확대
- 경력·과목·지역·비용 기준 후보 추천
- 학교·기관의 간편 수업 신청 링크
- 다중 교육업체를 지원하는 SaaS 구조

## 15. MVP 검증 계획

### 15.1 도입 전 기준선 측정

최소 1개월 동안 다음 항목을 기록한다.

- 수업 1건당 단체 공지 횟수
- 수업 1건당 개별 카카오톡·전화·문자 횟수
- 최초 공지부터 후보 확보까지 걸린 시간
- 최초 공지부터 최종 배정까지 걸린 시간
- 미응답 강사 수
- 강사 미확보로 지연·취소된 수업 수

### 15.2 MVP 도입 후 비교

- 관리자의 개별 연락 횟수 변화
- 최초 알림 후 기한 내 응답률
- 후보 확보 시간 변화
- 최종 배정 시간 변화
- 관리자와 강사의 사용 만족도
- 내부 알림·웹 푸시 진입률과 응답 완료율

### 15.3 성공 판단

다음 조건을 충족하면 배정 기능의 초기 가설이 검증된 것으로 본다.

- 수업당 개별 연락 횟수가 기존 대비 70% 이상 감소한다.
- 대부분의 수업이 최초 알림 1회와 선택 재알림 1회 이내에 후보를 확보한다.
- 관리자가 단체 카카오톡의 응답을 다시 정리하지 않아도 된다.
- 강사가 모바일에서 별도 설명 없이 응답을 완료할 수 있다.

## 16. 주요 위험과 대응

| 위험 | 영향 | 대응 방향 |
| --- | --- | --- |
| 강사가 알림을 보고도 응답하지 않음 | 배정 지연 지속 | 마감 시간 강조, 미응답자 필터, 선택 재알림과 카카오톡 공유 제공 |
| 웹 푸시 권한 미허용·미지원 | 즉시 도달률 저하 | 내부 알림을 기본 원장으로 유지하고 강사센터에서 푸시 활성화를 안내 |
| 응답 링크의 로그인 과정이 복잡함 | 응답률 저하 | 유효기간이 있는 개인별 보안 링크 우선 적용 |
| 조건부 가능 사유가 제각각임 | 후보 비교 어려움 | 초기에는 자유 입력, 사용 데이터를 보고 구조화 항목 추가 |
| 알림 발송 실패 | 강사가 요청을 받지 못함 | 발송 성공·실패 상태와 재발송 기능 제공 |
| 일정 충돌 데이터가 불완전함 | 중복 배정 가능 | 초기에는 플랫폼 확정 일정만 기준임을 명시하고 경고 중심으로 운영 |
| 월 10건 미만이라 데이터가 적음 | 검증 기간 장기화 | 최소 2~3개월 운영 데이터와 관리자 인터뷰를 함께 평가 |
| 기능 범위가 종합 ERP로 급격히 확대됨 | 개발 지연과 핵심 가치 약화 | Phase 1 성공지표 확인 전 후속 기능 확장 제한 |

## 17. 미결정 사항

다음 항목은 상세 설계 또는 개발 착수 전에 확정해야 한다.

- 서비스명과 브랜드 아이덴티티
- 회원가입 인증 방식
- 개인별 응답 링크의 추가 본인 확인 수준
- 향후 알림톡 도입 판단 지표, 발송 사업자 및 템플릿 정책
- 수업 정보 수정 시 재응답이 필요한 변경 범위
- 응답 마감 후 관리자의 수동 연장 허용 여부
- 조건부 가능 사유의 구조화 여부
- 미선택 강사에게 보여줄 안내 문구와 공개 정보 범위
- 관리자 수와 최고 관리자 권한 이관 정책
- 개인정보 보관 및 탈퇴 회원 데이터 처리 기간

## 18. 변경 관리 원칙

- 본 문서는 현재까지 합의된 제품 요구사항의 기준 문서로 사용한다.
- 문구 개선이나 구현 세부사항만으로는 제품 요구사항을 변경하지 않는다.
- 사용자 범위, 핵심 흐름, 권한, 기능 범위, 비즈니스 규칙이 변경될 때 문서를 갱신한다.
- 변경 시 버전, 날짜, 변경 이유, 영향을 받은 항목을 기록한다.

### 변경 이력

| 버전 | 날짜 | 변경 내용 |
| --- | --- | --- |
| 0.1 | 2026-08-08 | 초기 문제 정의, MVP 범위, 사용자·권한, 배정 정책, 성공지표 및 디자인 방향 작성 |
| 0.2 | 2026-08-08 | 수업별 주강사 1명·보조강사 0~2명 구성과 역할별 모집·응답·배정 규칙 추가 |
| 0.3 | 2026-08-08 | 주강사·보조강사 수업료 분리 입력과 모집·응답·배정·변경 이력 표시 규칙 추가 |
| 0.6 | 2026-08-09 | 운영센터 로그아웃, 관리자 초대·권한·상태 관리, 보안 세션과 활동 기록 요구사항 추가 |
| 0.7 | 2026-08-12 | 알림톡 필수를 내부 알림+무료 웹 푸시 기본, 카카오톡 공유 보조, 알림톡 선택 확장으로 변경하고 실제 응답 집계·선택 재알림 요구사항 명확화 |
